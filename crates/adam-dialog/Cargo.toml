[package]
name = "adam-dialog"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Predictable Kazakh dialog layer — 26-intent recogniser, multi-turn session, FST-backed slot expansion, session-aware retrieval composition, BeliefState with auditable contradiction resolution (v4.1.0)"

[dependencies]
adam-kernel = { path = "../adam-kernel" }
adam-kernel-fst = { path = "../adam-kernel-fst" }
adam-retrieval = { path = "../adam-retrieval" }
adam-reasoning = { path = "../adam-reasoning" }
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
toml.workspace = true

[dev-dependencies]
criterion = { version = "0.5", default-features = false, features = ["cargo_bench_support"] }

[[bench]]
name = "turn_latency"
harness = false
