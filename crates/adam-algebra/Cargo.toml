[package]
name = "adam-algebra"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Agglutinative algebra — Stage 1 of v6.2.0 neurosymbolic redesign. Lifts adam_kernel_fst's typed morphology (Case / Number / Possessive / Predicate / Tense / Voice / Person) into a uniform `SuffixOp` enum + `Composition` type with documented algebra rules (identity, ordered composition, idempotence, inverse, FST round-trip)."

[dependencies]
adam-kernel-fst = { path = "../adam-kernel-fst" }
serde.workspace = true
thiserror.workspace = true

[lints]
workspace = true
