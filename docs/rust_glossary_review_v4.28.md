# Rust Glossary — Native-Speaker Review (v4.28.0)

This document lists the **69 auto-curated `programming_rust.jsonl` entries** (rust_111…rust_179) that were added by Claude in releases v4.26.0 / v4.26.5 / v4.27.0 / v4.27.5. Their `reviewer` field is currently set to `claude`, which is an honest audit trail — they have NOT been reviewed by a native Kazakh speaker.

## Why this review matters

Per `data/raw/rust_book_kk/README.md`:

> The translations are first-pass drafts produced by Claude Opus 4.7 (the assistant), with terminology decisions locked in v4.7.0. **Native-speaker review is needed for technical accuracy and idiomatic phrasing.**

Each entry below contains:
- The `kk` text (what adam shows in responses)
- The `subject` field (the retrieval-key — must match how users naturally refer to the concept)
- The `object` field (what kind of thing the subject IS)

## Review workflow

For each entry:
1. **Approve as-is** → flip `reviewer: "claude"` → `reviewer: "shaman"` in `data/world_core/programming_rust.jsonl`. Adam will keep using the same text.
2. **Approve with edit** → fix `kk` text in the JSONL line, then flip `reviewer`. Edits will reach runtime via the next `cargo run --release --bin extract_facts` + `run_reasoner` cycle.
3. **Reject** → flip `review_status: "approved"` → `review_status: "rejected"`. Entry stops appearing in `facts.json`.

Use `bash scripts/refresh_readme_badges.sh` to verify metric counts after the review pass.

---

## Entries (69 total)

| ID | Subject | Object (IsA) | KK text |
|---|---|---|---|
| `rust_111` | `ownership` | жады басқару моделі | `ownership` — `Rust`-тың жадыны қауіпсіз басқару моделі: әрбір мән бір ғана иеленушіге тиесілі болады, иеленуші скоптан шыққанда мән автоматты түрде босатылады. |
| `rust_112` | `borrow` | сілтеме беру операциясы | `borrow` — `Rust`-та мәнді көшірмей сілтеме арқылы пайдалануға беру: `&value` оқу үшін, `&mut value` өзгерту үшін. |
| `rust_113` | `borrow checker` | статикалық анализатор | `borrow checker` — `Rust` компиляторының бөлігі: ережелерді тексеретін статикалық анализатор. Ережелер: бір уақытта бір өзгермелі сілтеме НЕМЕСЕ кез келген санды тұрақты сілтеме; сілтеме мерзімі мәннің мерзімінен ұзақ болмауы керек. |
| `rust_114` | `lifetime` | сілтеме мерзімі | `lifetime` — `Rust`-та сілтеменің жарамды болатын скоп ұзақтығы. Көбіне компилятор автоматты түрде шығарады; қажет жағдайларда `'a` синтаксисімен ашық белгіленеді. |
| `rust_115` | `trait` | интерфейс анықтамасы | `trait` — `Rust`-та типтердің ортақ мінез-құлқын анықтайтын құрылым: әдістер мен байланысты типтер тізімі. Типтер `impl Trait for Type` арқылы трейтті жүзеге асырады. |
| `rust_116` | `impl` | тілдік құрылым | `impl` — `Rust`-та типке әдістер мен байланысты функцияларды қосатын блок. Сондай-ақ трейтті жүзеге асыру үшін `impl Trait for Type { ... }` түрінде қолданылады. |
| `rust_117` | `match` | үлгіге сай келтіру өрнегі | `match` — `Rust`-тың үлгіге сай келтіру өрнегі: бір мәнді бірнеше шаблонға тексеріп, сай келген тармақтың кодын орындайды. Барлық мүмкін жағдайлар қамтылуы керек. |
| `rust_118` | `generic` | тип параметрі | `generic` — `Rust`-та параметрленген типтер мен функциялар: бір код бірнеше типпен жұмыс істейді. Синтаксис `<T>`, шектеулер `<T: Trait>` немесе `where T: Trait` арқылы көрсетіледі. |
| `rust_119` | `generics` | тип параметрлеу жүйесі | `generics` — `Rust`-та жалпыламалардың жинақты атауы: типтермен (`<T>`) және өмір мерзімімен (`<'a>`) параметрленген код. Сол бір функция немесе структ әртүрлі типтермен жұмыс істей алады. |
| `rust_120` | `closure` | анонимді функция | `closure` — `Rust`-та анонимді функция: айналадағы скоптан айнымалыларды ұстап ала алады. Синтаксисі `\|args\| body`. `Fn`, `FnMut` және `FnOnce` трейттері қолданылады. |
| `rust_121` | `iterator` | элемент шығарушы | `iterator` — `Rust`-та элементтерді бір-бірлеп шығаратын объект: `Iterator` трейтін жүзеге асырады, негізгі әдісі `next() -> Option<Item>`. Тұтынылмайынша есептелмейтін жалқау орындалу. |
| `rust_122` | `pattern` | деструктуризация үлгісі | `pattern` — `Rust`-та `match`, `let`, `if let` және функция параметрлерінде қолданылатын құрылым: мәнді деструктуризациялап, оның бөліктеріне атау береді. |
| `rust_123` | `move` | иелік ауыстыру | `move` — `Rust`-та мән иелігін жаңа айнымалыға толық ауыстыру: ескі айнымалы енді жарамды емес. Кейбір контекстерде ашық `move` кілт сөзі қолданылады (мысалы, `closure`-да). |
| `rust_124` | `module` | код ұйымдастыру бірлігі | `module` — `Rust`-та кодты топтастыратын ұйымдастыру бірлігі: `mod name { ... }` түрінде анықталады. `pub` модификаторы ашық қол жетімділікті береді. |
| `rust_125` | `crate` | компиляция бірлігі | `crate` — `Rust`-та компиляция бірлігі: бинарь немесе кітапхана. Бір крейтте бірнеше модуль бола алады. `cargo` крейттерді ұйымдастырады. |
| `rust_126` | `struct` | құрама деректер типі | `struct` — `Rust`-та байланысты деректерді топтастыратын құрылым: атаулы өрістер, кортеж-структ немесе бірлік структ түрінде анықталады. `impl` блогында әдістер қосылады. |
| `rust_127` | `enum` | нұсқалары бар тип | `enum` — `Rust`-та бірнеше нұсқаны қамтитын тип: әр нұсқа өз деректерін көтере алады. `match` өрнегімен әр нұсқа өңделеді. |
| `rust_128` | `function` | код блогы | `function` — `Rust`-та `fn name(params) -> ReturnType { body }` синтаксисімен анықталатын код блогы. Бағдарлама `main` функциясынан басталады. |
| `rust_129` | `mutability` | өзгертуге рұқсат қасиеті | `mutability` — `Rust`-та айнымалының өзгертуге рұқсат етілгенін көрсету: `let mut x` өзгермелі, `let x` тұрақты. Сілтемелерде де `&mut T` пен `&T` ажыратылады. |
| `rust_130` | `shadowing` | айнымалы жабу әдісі | `shadowing` — `Rust`-та сол бір атаудағы жаңа айнымалы алдыңғыны жабуы: типі мен мәні өзгере алады, бірақ ол жаңа байланыс. Айырмашылық `mut`-тан: `shadowing` жаңа айнымалы жасайды. |
| `rust_131` | `reference` | жад адресі | `reference` — `Rust`-та мәннің адресі: `&T` тұрақты сілтеме (тек оқу), `&mut T` өзгермелі сілтеме (өзгертуге рұқсат). Сілтемелер қарызға алу ережелеріне бағынады. |
| `rust_132` | `trait object` | динамикалық полиморфизм | `trait object` — `Rust`-та динамикалық полиморфизм механизмі: `dyn Trait` арқылы бір трейтті жүзеге асыратын әртүрлі типтерді бір айнымалыда сақтауға мүмкіндік береді. |
| `rust_133` | `box` | ақылды сілтеме | `Box` — `Rust`-та үймеде сақталатын мәнге арналған ең қарапайым ақылды сілтеме: `Box<T>` тип `T` мәнін стектен үймеге орналастырады. |
| `rust_134` | `smart pointer` | сілтеме типі | `smart pointer` — `Rust`-та қосымша мінез-құлық қосатын сілтеме типі: автоматты тазарту, есеп санағыш, серіктестікке қол жеткізу. Мысалдары: `Box<T>`, `Rc<T>`, `Arc<T>`, `RefCell<T>`. |
| `rust_135` | `rc` | санағышты ақылды сілтеме | `Rc` — `Rust`-та бір ағындағы ортақ иелік үшін есеп санағышы бар ақылды сілтеме: `Rc<T>`. Бір мәнге бірнеше иеленушіге рұқсат береді. |
| `rust_136` | `arc` | атомдық санағышты сілтеме | `Arc` — `Rust`-та көп ағынды контекстегі ортақ иелік үшін атомдық санағыш бар ақылды сілтеме: `Arc<T>`. `Rc`-нің ағындар арасында қауіпсіз нұсқасы. |
| `rust_137` | `refcell` | ішкі өзгермелілік сілтемесі | `RefCell` — `Rust`-та компиляция кезінде емес, орындалу кезінде тексерілетін иелік ережелерін ұсынатын ақылды сілтеме: `RefCell<T>`. Тұрақты сілтеме арқылы өзгертуге мүмкіндік береді. |
| `rust_138` | `mutex` | синхрондау примитиві | `Mutex` — `Rust`-та көп ағынды контекстегі ортақ деректерге қауіпсіз қол жеткізуді қамтамасыз ететін синхрондау примитиві: `Mutex<T>`. Бір ағын деректерге кезек-кезек қол жеткізеді. |
| `rust_139` | `send` | автоматты трейт | `Send` — `Rust`-та типтердің ағындар арасында иелігін ауыстыруға рұқсат ететін авто-трейт. Барлық қарапайым типтер `Send` жүзеге асырады; `Rc<T>` жасамайды, ал `Arc<T>` жасайды. |
| `rust_140` | `sync` | автоматты трейт | `Sync` — `Rust`-та типтерге сілтеменің ағындар арасында ортақ пайдаланылуына рұқсат ететін авто-трейт. `&T: Send` болса, `T: Sync` дегенді білдіреді. |
| `rust_141` | `async` | асинхрондық белгісі | `async` — `Rust`-та асинхронды функция мен блокты белгілейтін ключевое сөз: `async fn` `Future`-ды қайтарады, орындалу `await` арқылы күтіледі. |
| `rust_142` | `error handling` | қателерді өңдеу тәсілі | `error handling` — `Rust`-тың қателерді өңдеу тәсілі: `Result<T, E>` қалпына келтірілетін қателер үшін, `panic!` қалпына келтірілмейтін бұзылулар үшін. Ерекшеліктер жоқ — қателер мәндер ретінде беріледі. |
| `rust_143` | `question mark operator` | қате тарату операторы | `question mark operator` — `Rust`-та `?` операторы: `Result` немесе `Option` мәнінде қатені автоматты түрде шақырушы функцияға қайтаратын қысқа жазу әдісі. |
| `rust_144` | `derive` | макрос атрибуты | `derive` — `Rust`-та типке стандартты трейттерді автоматты түрде жүзеге асыратын макрос: `#[derive(Debug, Clone, PartialEq)]` түрінде қолданылады. Қол жетімді трейттер: `Debug`, `Clone`, `Copy`, `PartialEq`, `Eq`, `Hash`, `Default`, т.б. |
| `rust_145` | `vector` | өсетін массив | `vector` — `Rust`-та өсетін, біртипті элементтер тізімі: `Vec<T>`. Үймеде сақталады, элементтер қосуға, өшіруге және индекспен қол жеткізуге рұқсат береді. |
| `rust_146` | `slice` | тілім сілтемесі | `slice` — `Rust`-та жиым немесе вектордың үздіксіз бөлігіне сілтеме: `&[T]` тұрақты тілім, `&mut [T]` өзгермелі тілім. Көшіру жасамайды. |
| `rust_147` | `tuple` | тіркес тип | `tuple` — `Rust`-та белгіленген ұзындықтағы әртүрлі типтер тізбегі: `(T1, T2, T3)`. Әр элементке `.0`, `.1`, `.2` арқылы қол жеткізіледі немесе `let (a, b, c) = ...` арқылы деструктуризацияланады. |
| `rust_148` | `option` | болуы мүмкін мән енамы | `Option` — `Rust`-та мәннің бар немесе жоқ екенін білдіретін енам: нұсқалары `Some(T)` және `None`. `null` орнына қолданылады, типтік қауіпсіздікті қамтамасыз етеді. |
| `rust_149` | `unsafe` | қауіпсіздік шектеулерін айналып өту блогы | `unsafe` — `Rust`-та компилятордың кейбір қауіпсіздік тексерулерін айналып өтуге рұқсат беретін блок: `unsafe { ... }`. Шикі сілтемелерді `dereference` істеу, `FFI` шақыру, өзгермелі `static`-ке қол жеткізу үшін қолданылады. |
| `rust_150` | `cargo.toml` | жоба манифест файлы | `Cargo.toml` — `Rust` жобасының манифест файлы: жоба атауын, нұсқасын, тәуелділіктерін, түзеткіш профильдерін және басқа метадеректерді сақтайды. |
| `rust_151` | `қарыз алу` | сілтеме беру операциясы | `қарыз алу` — `Rust`-та мәнді көшірмей сілтеме арқылы пайдалануға беру: `&value` оқу үшін, `&mut value` өзгерту үшін. Иеленушіні ауыстырмай, тек уақытша қол жеткізу. |
| `rust_152` | `жад тазарту` | иелік әрекеті | `Rust`-та жад тазарту иеліктің скоптан шығуы кезінде автоматты түрде орындалады: `Drop` трейті іске қосылып, мән жадтан босатылады. Бұл үшін `garbage collector` те, қолмен `free()` те қажет емес. |
| `rust_153` | `let` | айнымалы жариялау кілт сөзі | `let` — `Rust`-та айнымалыны жариялайтын кілт сөз: `let x = 5;`. Үнсіз тұрақты — `mut` модификаторы өзгермеліге мүмкіндік береді. |
| `rust_154` | `mut` | өзгерілуі рұқсат модификаторы | `mut` — `Rust`-та айнымалының өзгертілуіне рұқсат беретін модификатор: `let mut x = 5;` болса, кейін `x = 10;` рұқсат етіледі. Үнсіз `let x` тұрақты. |
| `rust_155` | `fn` | функция жариялау кілт сөзі | `fn` — `Rust`-та функцияны жариялайтын кілт сөз: `fn name(params) -> ReturnType { body }`. Бағдарлама `main` функциясынан басталады. |
| `rust_156` | `references` | жад адресі | `references` — `Rust`-та сілтемелердің жинақты атауы: `&T` тұрақты сілтеме, `&mut T` өзгермелі сілтеме. Қарызға алу ережелеріне бағынады. |
| `rust_157` | `thread` | параллель орындалу бірлігі | `thread` — `Rust`-та параллель орындалу бірлігі: `std::thread::spawn(\|\| { ... })` арқылы құрылады. Деректер ағындар арасында `Send` трейті арқылы тасымалданады. |
| `rust_158` | `macro` | код генерациялау құрылымы | `macro` — `Rust`-та компиляция кезінде кодты генерациялайтын құрылым: `println!`, `vec!`, `format!` сияқты декларативті макростар; `derive`, `attribute`, `function-like` процедуралық макростар. |
| `rust_159` | `hello world` | қарапайым бағдарлама үлгісі | `Hello World` — `Rust`-та ең қарапайым бағдарлама: `fn main() { println!("Hello, world!"); }`. `cargo new` арқылы жоба құрылғанда автоматты түрде осы код жасалады. |
| `rust_160` | `тест` | автоматты тексеру функциясы | `тест` — `Rust`-та автоматты тексеру функциясы: `#[test]` атрибутымен белгіленеді, `cargo test` командасымен орындалады. Бірлік тесттер `#[cfg(test)]` модулінде, интеграциялық тесттер `tests/` каталогында. |
| `rust_161` | `expression` | мән қайтаратын құрылым | `expression` — `Rust`-та мән қайтаратын құрылым: арифметикалық операция, функция шақыру, блок (`{ ... }`), `match` өрнегі. `Rust` өрнекке негізделген тіл. |
| `rust_162` | `statement` | нұсқау құрылымы | `statement` — `Rust`-та мән қайтармайтын нұсқау: `let x = 5;` жариялау, нүктелі үтірмен аяқталатын өрнек. `Statement` мән береді, бірақ оны қолдануға болмайды. |
| `rust_163` | `deref coercion` | автоматты түрлендіру | `deref coercion` — `Rust`-та компилятордың `&T`-ны `&U` сілтемесіне автоматты түрлендіруі, егер `T` `Deref<Target=U>` трейтін жүзеге асырса. Мысалы: `&String` сілтемесі `&str`-ге, `&Box<T>` сілтемесі `&T`-ге айналады. |
| `rust_164` | `type alias` | тип балама атауы | `type alias` — `Rust`-та типке балама атау беру: `type Kilometers = i32;`. Жаңа тип жасамайды, тек оқудың жеңілдігі үшін, ұзын типтерді қысқартуға қызмет етеді. |
| `rust_165` | `where clause` | шектеу синтаксисі | `where clause` — `Rust`-та жалпылама шектеулерді жариялаудың кеңейтілген синтаксисі: `fn name<T>() where T: Trait { ... }`. Қарапайым `<T: Bound>` синтаксисіне қарағанда күрделі шектеулерді оқылымды жазуға мүмкіндік береді. |
| `rust_166` | `dynamic dispatch` | орындалу-кезіндегі полиморфизм | `dynamic dispatch` — `Rust`-та орындалу кезінде шешілетін `trait object` полиморфизмі: `dyn Trait` арқылы. `vtable` арқылы жанама шақыру; икемді бірақ аздап шығынды. |
| `rust_167` | `static dispatch` | компиляция-кезіндегі полиморфизм | `static dispatch` — `Rust`-та компиляция кезінде шешілетін жалпылама полиморфизм: `<T: Trait>` арқылы. Әр пайдаланылған тип үшін мономорфизация жасалады; шығынсыз жоғары өнімділік. |
| `rust_168` | `zero-cost abstraction` | тіл принципі | `zero-cost abstraction` — `Rust`-тың негізгі принципі: жоғары деңгейлі абстракциялар (итератор, `generic`, `trait`) қолмен жазылған төмен деңгейлі кодтан баяу болмайды. Компилятор оларды толық оптимизациялайды. |
| `rust_169` | `raii` | ресурс басқару тәсілі | `RAII` (`Resource Acquisition Is Initialization`) — ресурс басқару тәсілі: ресурс объектіні құру кезінде алынады, объекті скоптан шыққанда `Drop` арқылы автоматты түрде босатылады. `Rust`-тың жад басқаруының іргетасы. |
| `rust_170` | `pin` | жад жылжытпау кепілдемесі | `Pin` — `Rust`-та мәнді жадтан жылжытпауға кепілдік беретін типтік қаптама: `Pin<P>`. `async`, `await` және өзіне сілтемелі құрылымдарға қажет. |
| `rust_171` | `rust design` | тілдік дизайн принциптері | `Rust` дизайны үш негізгі мақсатқа бағытталған: жад қауіпсіздігі (`borrow checker` арқылы), қауіпсіз қатарластық (`Send` мен `Sync` арқылы), нөлдік құнды абстракциялар. Жинақтаушы тіл, манипуляция жоқ — әр шешім ашық және компиляция кезінде тексеріледі. |
| `rust_172` | `future` | асинхронды операция трейті | `Future` — `Rust`-та асинхронды операцияны білдіретін трейт: уақытпен өзгеретін мәнді сипаттайды. `poll` әдісі арқылы орындалады; `await` синтаксисі `Future`-ды күтеді. |
| `rust_173` | `дизайн` | тілдік дизайн принциптері | `Rust` дизайны үш мақсатқа бағытталған: жад қауіпсіздігі (`borrow checker` арқылы), қауіпсіз қатарластық (`Send` мен `Sync` арқылы), нөлдік құнды абстракциялар. Жинақтаушы тіл, манипуляция жоқ — әр шешім ашық және компиляция кезінде тексеріледі. |
| `rust_174` | `implicit type` | тип шығару тәсілі | `implicit type` пен `explicit type` — `Rust`-та тип жариялаудың екі тәсілі: айқын — `let x: i32 = 5;`, ал имплициттік — `let x = 5;` (компилятор контекстен шығарады). `Rust` көп жағдайда типті автоматты түрде анықтай алады. |
| `rust_175` | `type inference` | автоматты тип анықтау | `type inference` — `Rust`-тың контексттен айнымалы типін автоматты анықтау қабілеті: `let x = 5;` болса, компилятор `x` типін `i32` (үнсіз) деп анықтайды. Қажет болғанда жеке белгілеуге болады. |
| `rust_176` | `дизай` | тілдік дизайн принциптері | `Rust` дизайны үш мақсатқа бағытталған: жад қауіпсіздігі (`borrow checker` арқылы), қауіпсіз қатарластық (`Send` мен `Sync` арқылы), нөлдік құнды абстракциялар. |
| `rust_177` | `implicit` | тип шығару тәсілі | `implicit` — `Rust`-та ашық белгіленбейтін тип немесе түрлендіру: `let x = 5;` болғанда компилятор `x` типін контекстен шығарады. Қарама-қарсысы `explicit` — типті бағдарламашы өзі белгілейді. |
| `rust_178` | `explicit` | тип шығару тәсілі | `explicit` — `Rust`-та ашық белгіленген тип немесе түрлендіру: `let x: i32 = 5;`, `value as u32`. Қарама-қарсысы `implicit` — компилятор автоматты түрде шығарады. |
| `rust_179` | `explicit type` | тип шығару тәсілі | `explicit type` — `Rust`-та ашық белгіленген тип: `let x: i32 = 5;`. Бағдарламашы типті өзі көрсетеді. Қарама-қарсысы `implicit type` — компилятор контекстен шығарады. |

---

*Generated by adam Claude as part of v4.28.0 native-speaker review preparation. Update entries directly in `data/world_core/programming_rust.jsonl`; this file is read-only.*

